type ButtonProps = {
  text: string;
  to: string;
  withIcon?: boolean;
};

type TextProps = {
  text: string;
};

type CardLocationProps = {
  name: string;
  price: number;
};

export type { ButtonProps, TextProps, CardLocationProps };
